export default defineAppConfig({
    ui: {
      toast: {
        slots: {
        //   root: 'relative  group overflow-hidden shadow-lg rounded-lg ring ring-default p-4 flex border-0 gap-2.5 focus:outline-none bg-white   outline-0'
        }
      }
    }
  })
